DEPLOYMENT 3 — COMBINATION MATRIX AND RELEASE GATES

Baseline required:
  bf689e0 Harden pantry quantity form and safety contracts

1. Extract SNS-Deployment-3-release-gates-changed-files.zip into the SNS
   repository root. Allow Windows to merge folders and replace files.

2. From C:\Users\tlove\OneDrive\Desktop\SNS run:

   git status --short
   python tools\run_release_gates.py

   Required result:
   SNS release gates: PASS — 4/4 gates, 550 cases, 0 failures

3. Stage only this deployment:

   git add .github\workflows\release-gates.yml .gitignore api_http.py ingredient_profiles.py release_matrix.py render.yaml supabase_gateway.py supabase\migrations\20260719011105_deduplicate_inventory_sync.sql test_api_http.py test_ko_activities.py test_release_matrix.py test_supabase_gateway.py tools\run_release_gates.py docs\DEPLOYMENT-3-COMBINATION-MATRIX-AND-RELEASE-GATES.md

4. Commit and push:

   git commit -m "Add production combination matrix and release gates"
   git push origin main

5. Confirm GitHub Actions reports "Production release gates" green. Render will
   run the same gates during its build and will not make a failing build live.

6. Confirm Supabase applies migration 20260719011105. Then import and save the
   combined 140-row pantry twice. Both saves must succeed without duplicate
   inventory rows.

